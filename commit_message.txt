feat(long-format): replicate Nushell ls -l output style

- Add rounded table borders (╭╮╰╯) matching Nushell aesthetic
- Add relative timestamps (e.g. "10 months ago") via formatRelativeTime()
- Add row index (#) column with 0-based numbering
- Split owner into separate user/group columns with real OS lookup
- Add cell padding (1 space each side) for Nushell-style spacing
- Center-align lowercase green header labels
- Add centerByWidth() helper for header alignment
- Add platform-specific owner/group resolution (owner_unix.go, owner_windows.go)
- Expand file extension lists for broader type detection
- Add O(1) extension lookup via extTypeMap in init()
- Update build.sh to compile full package instead of single file

BREAKING CHANGE: -l output format changed — columns reordered to #/name/mode/num_links/user/group/size/modified, table borders and header style updated
